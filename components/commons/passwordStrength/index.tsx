import React, { ReactElement } from "react";
import { StyledStrength, StyledStrengthContainer } from "./styles";

const StrengthPassword = (): ReactElement => {
    return (
        <StyledStrengthContainer>
            <StyledStrength />
        </StyledStrengthContainer>
    );
};

export default StrengthPassword;
